name: app_desktop
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^16.0.0
  flutter_single_instance: ^1.6.0
  sqflite: ^2.4.2
  graphql_flutter: ^5.1.2
  intl: ^0.20.2
  user_activity_plugin:
    path: plugins/user_activity_plugin
  window_manager: ^0.5.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
